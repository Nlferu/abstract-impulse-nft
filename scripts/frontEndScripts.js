/**
@dev List of scripts to be coded for frontEnd and backEnd calls

Scripts:

getHighestBidder(frontend)
getHighestBid(frontend)
safeTransferFrom(frontend)
placeBid(frontend)

mintNFT (backend)
approve(backend)
acceptBid(backend)
withdrawMoney(backend)
renewAuction(backend)

*/
